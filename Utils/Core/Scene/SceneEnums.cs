namespace Utils.Core.Scene {
	public enum Status {
		ACTIVE,
		INACTIVE
	}
	public enum Timing {
		BEFORE,
		DURRING,
		AFTER,
		NEVER
	}
	public enum DrawType {
		CUSTOM,
		WITH_CAMERA,
		WITHOUT_CAMERA,
		NEVER
	}

}
